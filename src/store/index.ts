import { createStore } from 'vuex'
import UserModule from '@/modules/users/store/UserModule'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    User: UserModule
  }
})
